import { describe, it } from 'bun:test';

describe('ResultsTable', () => {
  it('pending T02: exposes the reviewer-visible results contract in the primary table', () => {
    throw new Error('Pending S03/T02 implementation.');
  });
});
