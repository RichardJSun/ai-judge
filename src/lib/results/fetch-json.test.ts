import { describe, it } from 'bun:test';

describe('fetchJson', () => {
  it('pending T02: throws reviewer-visible errors for failed HTTP responses', () => {
    throw new Error('Pending S03/T02 implementation.');
  });
});
